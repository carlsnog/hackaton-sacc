# Snapshot para deploy

O arquivo `vozes_ausentes_pb.sqlite3` desta pasta e uma copia somente leitura dos dados publicados pelo pipeline.

Atualize o snapshot antes de publicar na Vercel:

```bash
make deploy-snapshot
```
